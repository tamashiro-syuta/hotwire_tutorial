<%# idがquotesのTurboFrame(@quotes)の先頭に、@quote(createしたquote)を追加 %>
<%= turbo_stream.prepend "quotes", partial: "quotes/quote", locals: { quote: @quote } %>

<%# 上と下の書き方はそれぞれ同じ挙動をする %>
<%#= turbo_stream.prepend "quotes", @quote %>

<%# idがQuote.new = 追加したTurboFrameを初期化(空のコンテンツで更新) %>
<%= turbo_stream.update Quote.new, "" %>

<%# flashがTurbo Streamレスポンスと連動するようにするには、flashをリストに前置きしたり、 %>
<%# flashコンテナのコンテンツを更新するようにTurboに指示する行を追加する必要がある %>
<%# flashを積み重ねて画面に表示させる  => prepend %>
<%# 一度に1つのflashを画面に表示させる => replace %>

<%# idがflashのDOMノードに layouts/flash パーシャルの内容をプリペンドするようTurboに指示 %>
<%= render_turbo_stream_flash_messages %>